# Python dependencies for bin/update_scholar_citations.py.
#
# Upstream al-folio's requirements.txt also pins nbconvert (for Jupyter notebook
# posts) and rendercv[full] (for the rendercv CLI). Neither is used here: this site
# has no notebooks and no CV page. Add them back if either becomes relevant.
pyyaml
scholarly
# scholarly (1.7.11, still the latest) imports bibtexparser.bibdatabase, which
# bibtexparser 2.0 removed, and it doesn't pin the dependency itself. Unpinned, every
# scrape since 2026-09-09 died on import. Drop this once scholarly supports 2.x.
bibtexparser<2
