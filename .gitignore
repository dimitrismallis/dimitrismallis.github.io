_site
.bundle
.sass-cache
.jekyll-cache
.jekyll-metadata
.DS_store
.ruby-version
.tweet-cache
vendor


# al-folio v1 upgrade tooling output
al-folio-upgrade-report.md

# Gemfile.lock IS committed (as upstream al-folio does): this is a deployed site,
# not a library, so CI must resolve the exact gem versions that were tested.
